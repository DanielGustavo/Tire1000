# Frontend do zero a partir do Figma

## Destination

O frontend (`frontend/`) reconstruído do zero a partir do design no Figma (arquivo "Tire-Mil", fileKey `gNZUfCRJ0aiHIkcy5sugtE`) — mobile-first primeiro, depois um passe de adaptação para desktop. Um design system extraído do Figma (tokens Tailwind de cor/tipografia + componentes base: Button, Field, Select, IconButton, Bullet) é a fundação sobre a qual toda tela é construída.

A informação de arquitetura (IA) segue o Figma, não as rotas atuais:
- Uma **Landing Page pública** nova, com **Sign in**/**Sign up** como modais sobre ela — substitui as rotas de página inteira `login.tsx`/`signup.tsx`.
- A **Homepage** passa a incluir a lista paginada de "Suas redações" (com os estados de processando/rejeitada/avaliada) — absorve o que hoje é `essay-history.tsx`, que deixa de ser uma rota separada.
- A compra de créditos vira o fluxo **price modal + redirect Stripe**, sem página avulsa — `credits.tsx` deixa de ser uma rota separada.
- `themes.tsx`, `theme-detail.tsx`, `essay-upload.tsx` (fluxo de câmera/upload) e `essay-result.tsx` (Correção) continuam existindo como telas próprias, reconstruídas visualmente.

Destino alcançado quando as 6 telas/fluxos abaixo estiverem implementadas batendo com o Figma, em mobile e em desktop:
1. Landing + autenticação (Sign in/Sign up modais)
2. Homepage (Temas em destaque + Suas redações paginado)
3. Listagem de temas (+ filtro)
4. Detalhe do tema (+ variante sem crédito)
5. Envio de redação (fluxo de câmera no mobile, fluxo de upload de arquivo no desktop — ambos entram por um modal de dicas)
6. Correção — resultado da Revisão/Avaliação (+ estados in-progress/loading)

## Notes

- **Exceção às normas padrão do Wayfinder**: este mapa carrega execução, não só decisão — cada ticket entrega a tela construída em código (mergeada em `frontend/`), não um documento de decisão. O usuário quer o frontend pronto ao final do mapa.
- Arquivo Figma: fileKey `gNZUfCRJ0aiHIkcy5sugtE` ("Tire-Mil"). Nós de referência: Design System `5:3`, Assets `5:1268`, Components `33:2`, Responsive/mobile `5:1551`, Desktop `106:1830`.
- Sempre carregar a skill `figma-design-to-code` antes de chamar `get_design_context`.
- Sempre invocar `/grilling` e `/domain-modeling` quando a resolução de uma ticket envolver decisão de comportamento/estado, não só estilo puro.
- Vocabulário do domínio em `CONTEXT.md` (Correção, Revisão, Avaliação, Eixo, Destaque). Contrato de API e casos de uso já implementados: `.scratch/tire1000-mvp/spec.md`.
- Convenção de teste deste repo (`CLAUDE.md`): testes vivem na camada de application; UI pura normalmente não pede teste dedicado — não criar teste de componente só porque existe uma tela nova, a menos que haja lógica não-trivial.
- `get_metadata` nos nós `5:1551` (mobile) e `106:1830` (desktop) sozinhos estoura o limite de tokens da ferramenta — ao abrir uma ticket, peça `get_metadata`/`get_design_context` só do nó da seção/tela específica, não do canvas inteiro.

## Decisions so far

- **[01-design-system](issues/01-design-system.md)**: design system implementado em `frontend/src/index.css` (tokens `@theme`: cor, sombra "hard" 2px, tipografia título/default/small) + `frontend/src/components/{Button,Field,Select,IconButton,Bullet}.tsx`. Peso de fonte via utilitário Tailwind (`font-bold`), não bakeado no token de tamanho. Ícones via `lucide-react` (novo devDep — os nomes de camada do Figma batem 1:1 com o pacote). Bullet confirmado **não-circular** por screenshot real do Figma, apesar do nome sugerir círculo — implementado com cantos retos. Fonte Lato via Google Fonts `<link>` em `index.html`.
- **[02-landing-e-autenticacao](issues/02-landing-e-autenticacao.md)**: `frontend/src/pages/landing.tsx` + `frontend/src/components/Modal.tsx` (novo componente de design system) + roteamento em `App.tsx`. `/` alterna entre `LandingPage`/`HomePage` conforme `getAccessToken()`; `/login`/`/signup` são rotas compartilháveis que abrem a LP com o modal correspondente já aberto (redirecionam pra `/` se já autenticado); fechar qualquer modal (backdrop/Esc) navega pra `/`. Cadastro é wizard de 2 passos: dados da conta → seleção de créditos (`creditsQty` só é enviado no `POST /auth/signup` depois que o usuário escolhe uma quantidade ou clica "Continuar sem comprar créditos agora" — a ADR-0005 citada na ticket foi substituída pela ADR-0009, checkout no signup é opt-in). Preço em R$ não é exibido nos botões de crédito (só existe no Stripe, ADR-0002) — só quantidade, igual `credits.tsx`. Login social (Google) do Figma não implementado, fora de escopo do MVP. Tokens novos: `--text-hero`, `--text-subtitle`, `--color-neutral-600`, `--color-alert-100`, `--color-error-100`; variantes novas em `Bullet` (`alert`/`error`/`info`/`pink`, os 2 últimos com hex cru por não serem estilo nomeado no Figma).
- **[03-homepage-e-historico](issues/03-homepage-e-historico.md)**: `frontend/src/pages/home.tsx` reescrito, absorve e deleta `essay-history.tsx` (rota `/essays` removida). Ícone de notas do header = **saldo de créditos** (não "Suas redações" como a ticket cogitava), único entry point do price modal (`components/PriceModal.tsx`, novo) — confirmado com o usuário. Paginação de "Suas redações" é client-side (`GET /essays` não pagina, fora de escopo do backend). `EssayDTO`/`toEssayDTO` ganhou `finalScore: number | null` (mudança aditiva no backend, confirmada com o usuário — Figma mostra a nota no card, o DTO da lista não tinha o campo). Classificação visual por status segue `RESENDABLE_STATUSES` (promovida de `essay-result.tsx` pra `essay-service.ts` junto com `REJECTION_REASON_LABELS`) em vez do agrupamento solto da ticket — `EVALUATION_FAILED` não é reenviável (ADR-0001). Componente novo `components/TexturedCard.tsx` (card com textura de pontos, reaproveitado em temas e redações — assets em `frontend/src/assets/card-texture{,-dark}.png`). `Bullet` ganhou `size?: "fixed"|"auto"` + variante `"white"`; `IconButton` ganhou `rotate?: "left"|"right"` (mesmo padrão do `Bullet`). Tokens novos: `--color-neutral-{20,200,500}`.
- **[04-listagem-de-temas](issues/04-listagem-de-temas.md)**: `frontend/src/pages/themes/themes.tsx` reescrito (migrado pra pasta por página; `theme-detail.tsx` continua flat, fora de escopo desta ticket). Filtro por eixo é **só o modal** (`ThemesFilterModal`) — confirmado via `get_design_context` do frame "Temas" (106:2006): não há chips de eixo inline na listagem, só busca + ícone que abre o modal. O modal do Figma mostra um Select single-value **e** uma "Tags List" com 3 chips removíveis simultâneos — mismatch com o backend, que só aceita um `topicId` singular em `GET /themes` (`list-themes.ts`/`theme-repository.ts`, sem suporte a multi-eixo). Decisão (usuário, via `/grilling`): implementar **só o Select** (com uma opção síntética "Todos os eixos" pra limpar o filtro) — a Tags List do Figma foi descartada, não é multi-seleção real, é dado de exemplo do componente genérico reusado. "Filtrar"/"Voltar" no modal são apply/cancel (estado pendente só aplica em "Filtrar"). Paginação client-side (`GET /themes` não pagina, mesmo padrão da ticket 03) — `EssaysPagination` promovida pra `components/Pagination.tsx` (nome genérico, sem dependência de Essay) já que agora tem 2 consumidoras. `IconButton` ganhou `size?: "default"|"large"` (mesmo padrão de `Bullet`/`rotate`) pro botão de filtro 48px do search bar.

- **[05-detalhe-do-tema](issues/05-detalhe-do-tema.md)**: `frontend/src/pages/themes/theme-detail.tsx` reescrito (migrado pra pasta por página, mesmo padrão de `home/`/`themes/`), substitui `theme-detail.tsx` flat. Frame "Tema" com/sem crédito do Figma são visualmente a mesma tela — a diferença é só destino do CTA: `handleStartEssay` navega pra `/essays/new?themeId=` se `userQuery.data.credits > 0`, senão abre `PriceModal` (mesmo componente da ticket 03). As duas variantes de botão do Figma ("Tirar foto"/"Fazer upload") ficam lado a lado sempre, ambas disparando o mesmo `handleStartEssay` — o ponto de entrada real do fluxo de câmera/upload (modal de dicas) é escopo da ticket 06, aqui só o CTA. Token novo `--text-subtitle-small` (20px/bold, estilo "subtitle small" do Figma, usado no título de cada Texto motivador). **Gap de backend fechado nesta ticket** (via `/grilling` com o usuário, decisão registrada — imagem de Texto motivador não tinha nenhum jeito de virar URL, `fileKey` chegava cru na API): novo bucket S3 privado `ThemeAssetsBucket` + distribuição CloudFront com Origin Access Control (`backend/sls/resources/theme-assets.yml`), env var `THEME_ASSETS_CDN_DOMAIN`; `toReferenceTextDTO` agora recebe `themeAssetsBaseUrl` e devolve `content.url` pronta pra `<img src>` em vez do `fileKey` cru (frontend nunca soube de bucket/CDN). Só a plumbing de código entra agora — deploy real da infra e upload do asset do seed (`seed/mapa-comunidades.png`) ficam como passo manual do usuário, documentado em `backend/README.md` (seção "Seeding themes/topics"); até lá a imagem do Texto motivador II aparece quebrada em produção/dev (confirmado via captura de tela nesta sessão, esperado). Lógica de dados/estado extraída pra `useThemeDetailPage.ts` (mesmo padrão de `useThemesPage.ts`); badges de ENEM/eixo promovidos pra `components/ThemeBadges.tsx` (2º consumidor, reaproveitado por `ThemeListItem`) — ambos ajustes do `/code-review` desta ticket. CTA fica desabilitado enquanto `currentUser` carrega, pra não mandar erroneamente pro price modal um usuário com crédito (`credits` só cai pra `?? 0` depois que a query resolve).
- **[06-fluxo-de-envio-de-redacao](issues/06-fluxo-de-envio-de-redacao.md)**: `frontend/src/pages/essay-upload/` reescrito (migrado pra pasta por página), substitui `essay-upload.tsx` flat. Decisões fechadas via `/grilling` com o usuário (a leitura literal da ticket levava a um design errado em pelo menos 2 pontos): (1) **sem UI de câmera própria** — o nó "Native camera" do Figma (`148:2588`) é só um placeholder pro app de câmera nativo do SO, acionado via `<input type="file" capture="environment">` (mesma técnica do `essay-upload.tsx` antigo), nunca `getUserMedia`/`MediaStream`/`<video>`; (2) detecção de permissão é best-effort — `navigator.permissions.query({name:"camera"})` onde suportado, e onde o estado é desconhecido/API ausente, um `getUserMedia({video:true})` descartável só como gatilho pra decidir "revoked" vs "accepted" (stream nunca usado pra captura real); modal "revoked" ganhou um botão que a ticket não tinha ("Ativei a permissão, tentar novamente", reabre a checagem); (3) **loading modal cobre só o `POST /essays` + upload S3**, não o polling de `GET /essays/{essayId}` até sair de VALIDATING como a ticket sugeria — sucesso navega pra Home (não pra `/essays/{id}`), e a modal de erro genérica só dispara em falha técnica do próprio upload (rede/API), nunca por REJECTED/VALIDATION_FAILED (que exigiriam o polling fora de escopo aqui). Os dois CTAs de `theme-detail.tsx` agora passam `?mode=camera`/`?mode=upload` pra selecionar a modal de entrada e a variante de botão da "Photo confirmation modal" ("Tirar outra foto" vs "Selecionar outro arquivo") — o caminho `upload` não usa o atributo `capture`, servindo também de entry point desktop (ticket 12) sem mudança. Extensões de design system: `Bullet` ganhou `size?: "large"|"small"` (56px/40px, os badges de ícone rotacionados dos modais, ao lado do `"fixed"` de 51px já existente) e `Button` ganhou variante `"error"` (bg `error-300`/texto branco, só usado pela modal de erro). **QA manual no navegador não foi possível** (mesma limitação da ticket 04 do outro mapa) — sem ferramenta de automação de browser disponível nesta sessão; validado só por `tsc`/`oxlint` limpos e revisão de código (`/code-review`, 2 achados reais corrigidos: erro de tamanho de foto não aparecia na modal de permissão aceita, e um tipo `"camera"|"upload"` duplicado entre hooks). **Bug reportado pelo usuário após o merge** (Firefox/Safari): esses browsers não suportam `navigator.permissions.query({name:"camera"})` (lança erro), então `checkCameraPermission` sempre caía no fallback via `getUserMedia`, que resolve silenciosamente quando a permissão já foi concedida antes — a modal "accepted" aparecia em toda visita, não só na primeira. Corrigido lembrando o outcome via `localStorage` (`essayUpload:cameraPermissionGranted`) nos browsers sem suporte à query.
- **[07-correcao-resultado](issues/07-correcao-resultado.md)**: `frontend/src/pages/essay-result/` reescrito (migrado pra pasta por página), substitui `essay-result.tsx` flat. Decisões fechadas via `/grilling` com o usuário: (1) highlight/comentário do avaliador (hover-only por `CONTEXT.md`) vira tap/click pra abrir/fechar nessa tela mobile-first, sem equivalente de hover (`HighlightedEssayText.tsx`); (2) a página só renderiza pra status pendentes e `SUCCESS` — os 4 status terminais sem sucesso redirecionam pra Home (`BLOCKED_ESSAY_RESULT_STATUSES`), já que é lá que o usuário age sobre eles; (3) reenvio de foto reusa o fluxo completo da ticket 06 em vez de um form simples — step machine extraído pra `useEssayCaptureFlow.ts` (compartilhado entre upload novo e resend), e o resend (`EssayResendFlow.tsx`/`useEssayResendFlow.ts`, em `pages/home/components/`) é disparado por estado local no `EssayCard`, não por rota (pedido explícito do usuário, ver ticket 08); (4) badges de "ENEM {ano}"/eixo no header do Figma exigiriam denormalizar mais 2 campos no `Essay` (mesma categoria de gap de backend da ticket 05) — usuário optou por **simplificar sem essas badges** em vez de fechar o gap. Cores de competência (`COMPETENCY_COLORS`) batem exatamente com os tokens do DS (primary-100/alert-100/error-100/info-300/pink-300). Novo componente de DS `components/PaperCard.tsx` (papel pautado — borda recortada via `radial-gradient` repetido em px fixo, não um asset esticado, pra não distorcer em nenhuma largura de card; linhas via CSS repeating em `em` amarrado ao `line-height` real do texto) e variante `Bullet` `"dark-outline"`. **QA manual no navegador não foi possível** (mesma limitação das tickets 04/06) — validado só por `tsc`/`oxlint`/build limpos e `/code-review`.
- **[08-desktop-landing-e-autenticacao](issues/08-desktop-landing-e-autenticacao.md)**: primeira ticket de desktop do map — estabelece o breakpoint `lg:` (1024px, default do Tailwind) como o único corte "desktop" usado daqui pra frente (tablet dedicado é fora de escopo do map). `frontend/src/pages/landing/{landing.tsx,components/{Header,Hero,Steps,Illustration,Cta}.tsx}` ganharam classes `lg:`. **Modais não mudaram** — `components/Modal.tsx` (ticket 02) já limitava a caixa a `max-w-[377px]` centralizada em qualquer largura de viewport, então já era o comportamento "modal pequeno centralizado" que o Figma desktop pede; `SignInModal`/`SignUpModal`/`CreditsStep` ficaram intocados. Achado real de layout: Steps do Figma desktop não é só a mobile mais larga — cada item vira um cartão escuro individual (`bg-neutral-900`/borda `neutral-0`/`shadow-hard-pink`/texto branco) sobre a faixa amarela, e o título da seção sobe de `text-title`(31px) pra `text-hero`(39px) só em `lg:`; Illustration muda de heading forçado em 3 linhas (mobile) pra 1 linha (desktop, via `<br className="lg:hidden">` com `{" "}` explícito pra não colar palavras) e de lista empilhada pra grid 2 colunas. Página raiz ganhou `lg:max-w-[1280px] lg:mx-auto` (largura nativa do frame Figma) pra Header/Illustration não esticarem sem limite em monitores muito largos — achado do `/code-review` (eixo Spec), corrigido antes do commit. `/code-review` (eixo Standards) não achou violação, só 2 smells de baixo risco não corrigidos: cartões escuros do Steps reimplementam inline o variant `"dark"` já existente em `Button`/`Bullet` em vez de reusar, e vários valores de pixel arbitrários novos sem token (`72px`/`372px`/`276px`/`309px`/`374px`/`700px`) — consistente com a convenção já usada no resto do código. QA manual em browser não foi possível (sem automação de browser disponível), validado só por `tsc`/`oxlint`/`vite build` limpos + revisão de código.
- **[08-modais-sem-rota](issues/08-modais-sem-rota.md)**: `/essays/new`, `/login` e `/signup` removidos de `App.tsx` — as 3 existiam só pra empilhar um modal, sem tela própria. `EssayUploadPage`/`useEssayUploadPage.ts` (`pages/essay-upload/`) viram `EssayUploadFlow.tsx`/`useEssayUploadFlow.ts` (`pages/themes/components/`), disparados por estado local (`uploadMode`) em `useThemeDetailPage.ts` — mesmo padrão do resend da ticket 07. `LandingPage` ganhou estado local `authModal`, repassado como callback pros pontos de entrada (`Header`/`Hero`/`Cta`) e pros modais (`SignInModal`/`SignUpModal`); `PublicOnlyRoute` removido (guarda redundante, já que `LandingPage` só renderiza deslogado). Sucesso de login/signup continua chamando `navigate("/")` mesmo já em `/` — necessário pra forçar `RootRoute` a reler `getAccessToken()` (sem contexto de auth reativo no app), mesmo padrão do sign-out em `AppLayout.tsx`.
- **[09-desktop-homepage](issues/09-desktop-homepage.md)**: `frontend/src/pages/home/{home.tsx,components/{ThemesSection,ThemeCard,EssaysSection}.tsx}` + `frontend/src/layouts/AppLayout.tsx` ganharam classes `lg:`. Layout vira 2 colunas via `lg:flex-row-reverse` num wrapper único em `home.tsx` (mantém ordem do DOM = mobile, só inverte visualmente) — "Suas redações" `lg:w-[847px] lg:shrink-0` à esquerda, "Temas" `lg:flex-1` à direita; o teto `lg:mx-auto lg:max-w-[1280px]` foi colocado só nesse wrapper da Homepage, não no `AppLayout` compartilhado (Header/Footer têm fundo próprio que fica melhor full-bleed; outras páginas em `AppLayout` ainda não têm ticket de desktop). Temas troca o carrossel horizontal (embla) por lista vertical estática em `lg:` (mesmos `ThemeCard`s, só `lg:w-full`), com "Ver todos temas" abaixo da lista somado ao "Ver todos" do topo que o mobile já tinha. Estado vazio centraliza verticalmente numa área `lg:h-[606px]`, igual ao frame "Homepage empty". **User menu (`195:391`) já existia** desde a ticket 03 (dropdown em `AppLayout.tsx`, disparado pelo ícone de usuário do header em qualquer largura — não era UI nova, ao contrário do que a ticket cogitava); só corrigido um bug pré-existente (negrito da saudação não cobria o "!"). **Decisão sem `/grilling` ao vivo** (sessão em background, sem usuário síncrono): os 2 frames desktop do Figma ("Homepage"/"Homepage empty") só mostram o ícone de usuário no header, sem o ícone de créditos que hoje é o único entry point do `PriceModal` (ticket 03) — mantido visível abaixo de `lg:` (mobile intocado) mas escondido (`lg:hidden`) em telas largas pra não presumir remoção de funcionalidade sem confirmação; usuário em desktop fica sem esse atalho direto, só compra crédito via o fluxo "sem crédito" do detalhe do tema (ticket 05) — fica pra confirmar/reverter. `/code-review` (2 achados reais corrigidos): `.map()` duplicado entre a versão carrossel/lista de `ThemesSection` (extraído pra variável reaproveitada) e o teto de 1280px que tinha ido pro `AppLayout` compartilhado em vez de escopado à Homepage. Nenhuma extensão nova de design system. QA manual em browser não foi possível (sem automação de browser disponível), validado só por `tsc -b`/`oxlint`/`vite build` limpos + `/code-review`.

- **[10-desktop-temas](issues/10-desktop-temas.md)**: `frontend/src/pages/themes/themes.tsx` ganhou `lg:` (teto `lg:mx-auto lg:max-w-[1280px]` escopado à página, `lg:px-10`, grid `grid-cols-1 lg:grid-cols-3` no lugar da coluna única) — mesmo padrão das tickets 08/09. `ThemeListItem`/`Pagination` reaproveitados sem alteração (tamanhos já batiam com o frame desktop do Figma). **Filtro por eixo continua modal** (`ThemesFilterModal`) — `get_design_context` no frame "Temas" desktop (195:1437) não mostra nenhum estado aberto de filtro, só o trigger fechado mais uma "Tags List" inline de 3 chips de exemplo (mesmo componente genérico com dado de exemplo já descartado na ticket 04, agora solto na página em vez de só no modal); não implementado, pra preservar a decisão de single-select da 04 sem reabrir sem usuário ao vivo. `Modal.tsx` já cabia no padrão desktop desde a ticket 08. **Questão aberta**: `THEMES_PER_PAGE` fica em 3 (igual mobile) mesmo o Figma mostrando grid 3x3 (9/página) — adaptar por breakpoint mudaria semântica de paginação entre viewports (decisão de comportamento, não só layout), documentada sem decidir por falta de usuário ao vivo. QA manual em browser não foi possível (mesma limitação das tickets anteriores).

- **[11-desktop-tema](issues/11-desktop-tema.md)**: `frontend/src/pages/themes/theme-detail.tsx` ganhou `lg:` (teto `lg:mx-auto lg:max-w-[1280px]` + `lg:px-10`, mesmo padrão das tickets 08/09/10). Coluna única vira 2 colunas em `lg:` — "Article Section" (back button + badges/título + textos motivadores) à esquerda, card de CTA fixo (295px, borda 2px, `shadow-hard`) à direita, substituindo a barra sticky de rodapé do mobile (que continua intocada abaixo de `lg:`). Card desktop ganha título/subtítulo novo do Figma ("Já finalizou sua redação?"/"Envie-a aqui...") sem equivalente no mobile. Imagem do Texto motivador II ganha largura fixa 434px centralizada em `lg:` (Figma não estica a imagem pra largura total da coluna). **Variante "sem crédito"**: confirmado via `get_design_context` que o Figma só tem uma frame "Tema" no canvas Desktop (`195:1882`), sem par "com/sem crédito" como o mobile (`207:3552`/`207:3677`) — tratado como confirmação de que o layout é idêntico em qualquer estado de crédito, preservando a decisão da ticket 05 (só o destino do `handleStartEssay` muda, lógica intocada). `/code-review`: 1 achado Standards de baixo risco corrigido (largura `434px` duplicada entre imagem/legenda, movida pro wrapper) e 1 achado Spec real corrigido (merge do bloco back-button/badges/título com a lista de textos tinha achatado os 2 gaps distintos do mobile num só `gap-6`, mudando espaçamento fora do escopo `lg:` — corrigido reaninhando os blocos, que também bate com o aninhamento Section title/Article Section do próprio Figma). QA manual em browser não foi possível (mesma limitação das tickets anteriores).

## Not yet specified

- Inventário completo de componentes-base ainda não descobertos no Figma (toast/alerta — existe paleta "Alert" no DS mas nenhum componente "Toast" no Components canvas ainda visto). Shell de modal/dialog resolvido na ticket 02 (`components/Modal.tsx`); card com textura + paginação numérica resolvidos na ticket 03 (`components/TexturedCard.tsx`, `EssaysPagination` em `home.tsx` — ainda local à Homepage, promover pra design system se outra tela precisar).
- Fallback de permissão de câmera negada no mobile (os modais "Camera permission revoked/accepted" existem no Figma, lógica de retry ainda não conversada) — cai dentro da ticket de envio de redação.
- Exportação de assets/ilustrações (nó `5:1268`) — ainda não inventariado; abrir quando alguma tela precisar de um asset específico.
- Conteúdo do menu hamburguer do header (nó `14:56`/`14:57`) — renderizado só como ícone visual na ticket 02, sem `onClick`; nenhum drawer/menu foi visto ainda no Components canvas do Figma.

## Out of scope

- Breakpoint de tablet dedicado — só mobile e desktop; o meio-termo fica por herança natural do CSS responsivo.
- Mudanças de lógica de negócio/casos de uso do backend — este mapa é só frontend visual e estrutural sobre as APIs já existentes (ver `.scratch/tire1000-mvp/spec.md`).
- Login social (Google) — já fora de escopo do MVP (ver spec do backend).
- Paginação/rate limiting de API — contrato de backend já definido; o frontend só consome o que existe.
